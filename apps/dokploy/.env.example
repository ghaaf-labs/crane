DATABASE_URL="postgres://dokploy:amukds4wi9001583845717ad2@localhost:5432/dokploy"
PORT=3000
NODE_ENV=development

# REQUIRED. Signs every session/cookie/reset token/API key. The app refuses to
# start without it (outside tests). Generate a unique value, e.g.:
#   openssl rand -hex 32
BETTER_AUTH_SECRET="change-me-generate-with-openssl-rand-hex-32"

# Set to "true" when serving over HTTPS so auth cookies are marked Secure.
# Leave unset/false for plain-HTTP or localhost development.
# SECURE_COOKIES=true
